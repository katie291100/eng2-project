jpa:
  default:
    properties:
      hibernate:
        hbm2ddl:
          auto: create-drop
videos:
  url: /videos

kafka:
  default:
    embedded:
      enabled: true
  streams:
    default:
      start-kafka-streams: true